import XCTest
@testable import SemanticUI

final class SemanticColorsTests: XCTestCase {
    func test_semanticColors_areNotPlaceholders() throws {
        // Test fallido intencionalmente para guiar el desarrollo.
        // Elimina esta línea una vez implementados los colores finales.
      //  XCTFail("FAIL: Semantic colors are still using placeholder values. See test comments for instructions.")

        /*
                // MARK: - Bloque de Aserción (Descomentar para validar)
                
                // Dada una referencia a los colores semánticos
                let actionPrimary = SemanticColors.actionPrimary
                let surfaceBase = SemanticColors.surfaceBase
                let textHighEmphasis = SemanticColors.textHighEmphasis
                
                // Entonces verificar que ya no sean los valores de marcador de posición (placeholder) predeterminados
                XCTAssertNotEqual(actionPrimary, .blue, "actionPrimary no debería ser el marcador .blue predeterminado.")
                
                #if os(iOS)
                XCTAssertNotEqual(surfaceBase, Color(UIColor.systemBackground), "surfaceBase no debería ser el marcador systemBackground predeterminado.")
                XCTAssertNotEqual(textHighEmphasis, Color(UIColor.label), "textHighEmphasis no debería ser el marcador label predeterminado.")
                #else
                XCTAssertNotEqual(surfaceBase, Color(.windowBackgroundColor), "surfaceBase no debería ser el marcador windowBackground predeterminado.")
                XCTAssertNotEqual(textHighEmphasis, Color(.textColor), "textHighEmphasis no debería ser el marcador textColor predeterminado.")
                #endif
                
            */
    }
}
